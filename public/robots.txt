# This site must not appear in search results. Crawling is deliberately
# ALLOWED so that crawlers can see the noindex directives (X-Robots-Tag
# header and <meta name="robots"> tag). Blocking with "Disallow: /" would
# hide those directives and let URLs surface in results as bare links.
User-agent: *
Disallow:
